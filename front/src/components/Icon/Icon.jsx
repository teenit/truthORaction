
import { SvgIcon } from "@mui/material";
import React from "react";


const Icon = (props) => {
  const {icon} = props;

  return(
    <div className="Icon">
      <SvgIcon />
    </div>
  )
}

export default Icon;